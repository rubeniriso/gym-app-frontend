import React from "react";

interface TrainingDayExerciseProps {}
//TODO: En este la idea es que sea inputs de formulario individual, como una fila de Excel con el ejercicio a elegir, fila, etc, etc.
const TrainingDayExercise = ({}: TrainingDayExerciseProps) => {
  return <div></div>;
};

export default TrainingDayExercise;
