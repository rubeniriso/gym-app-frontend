import React from "react";
import { ServerComponent } from "../components/ServerComponent";

const Page = () => {
  return (
    <div>
      <ServerComponent />
    </div>
  );
};

export default Page;
